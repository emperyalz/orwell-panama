import {internalMutation,internalQuery} from './_generated/server';
import {v} from 'convex/values';
import {paginationOptsValidator} from 'convex/server';
export const candidates=internalQuery({args:{paginationOpts:paginationOptsValidator},handler:(ctx,{paginationOpts})=>ctx.db.query('activity').order('desc').paginate(paginationOpts)});
export const visibleCandidates=internalQuery({args:{},handler:async ctx=>{const people=await ctx.db.query('politicians').collect();const rows=await Promise.all(people.map(async person=>Promise.all(['news','social'].map(kind=>ctx.db.query('activity').withIndex('by_politician_date',q=>q.eq('politicianId',person._id)).order('desc').filter(q=>q.eq(q.field('kind'),kind)).take(4)))));return rows.flat(2).filter(row=>!row.imageUrl?.includes('.convex.cloud/')||!row.sourceLogoUrl&&row.kind==='news').map(row=>row._id);}});
export const byIds=internalQuery({args:{ids:v.array(v.id('activity'))},handler:async(ctx,{ids})=>(await Promise.all(ids.slice(0,25).map(id=>ctx.db.get(id)))).filter((row):row is NonNullable<typeof row>=>row!==null)});
export const update=internalMutation({args:{id:v.id('activity'),imageUrl:v.optional(v.string()),sourceLogoUrl:v.optional(v.string()),mediaKind:v.optional(v.union(v.literal('image'),v.literal('video')))},handler:async(ctx,{id,...fields})=>{const patch=Object.fromEntries(Object.entries(fields).filter(([,value])=>value!==undefined));if(Object.keys(patch).length)await ctx.db.patch(id,patch);}});
