import {ArrowUpRight,FileText,Plus} from 'lucide-react';
import type {ExtractedPropuesta} from '@/lib/types';
export function PolicyProposalCard({data,pdfUrl}:{data:ExtractedPropuesta;partyCode:string;pdfUrl?:string}){
 return <article className="policy-plan"><header><div><span className="eyebrow">Documento de propuestas</span><h3>Plan de Gobierno</h3></div>{pdfUrl&&<a className="record-button" href={pdfUrl} target="_blank" rel="noreferrer"><FileText size={16}/>Consultar original <ArrowUpRight size={16}/></a>}</header>{data.resumenEjecutivo&&<p className="policy-summary">{data.resumenEjecutivo}</p>}{data.temasPrioritarios.length>0&&<div className="policy-topics">{data.temasPrioritarios.map(t=><span key={t}>{t}</span>)}</div>}
 <div className="policy-columns"><section><h4>Áreas estratégicas</h4>{data.areasEstrategicas.map((area,i)=><details className="policy-area" key={area.area} open={i===0}><summary><span>{String(i+1).padStart(2,'0')}</span><strong>{area.area}</strong><Plus size={18}/></summary><ul>{area.propuestas.map((p,j)=><li key={j}>{p}</li>)}</ul></details>)}</section><aside>{data.promesasClave.length>0&&<section><h4>Compromisos publicados</h4><ol className="policy-promises">{data.promesasClave.map((p,i)=><li key={i}><span>{String(i+1).padStart(2,'0')}</span><p>{p}</p></li>)}</ol></section>}{data.gruposBeneficiarios.length>0&&<section><h4>Grupos beneficiarios</h4><p>{data.gruposBeneficiarios.join(' · ')}</p></section>}{data.presupuestoMencionado&&<section><h4>Presupuesto mencionado</h4><p>{data.presupuestoMencionado}</p></section>}<p className="source-note">{data.tieneIndicadores?'El documento incluye indicadores.':'El documento no incluye indicadores identificados.'} Los compromisos no acreditan resultados de gestión.</p></aside></div></article>;
}
